<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fivecontacts">


    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.ANSWER_PHONE_CALLS"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >
        <activity android:name=".ContatosEmergencia.activities.PerfilUsuario_Activity"></activity>
        <activity android:name=".ContatosEmergencia.activities.NovoUsuario_Activity"></activity>
        <activity android:name=".ContatosEmergencia.activities.AlterarContatos_Activity"></activity>
        <activity android:name=".ContatosEmergencia.activities.ListaDeContatos_Activity"></activity>
        <activity android:name=".ContatosEmergencia.activities.ChecagemLogin_Activity">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

    </application>

</manifest>